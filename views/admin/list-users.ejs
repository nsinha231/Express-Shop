<title>(<%=users.length%>) List of Users</title>
<div class="table-responsive py-5 mb-5">
  <table class="table table-hover">
    <thead class="thead-dark">
      <tr>
        <th scope="col">#</th>
        <th scope="col">Avatar</th>
        <th scope="col">Username</th>
        <th scope="col">Email</th>
        <th scope="col">Delete</th>
      </tr>
    </thead>
    <tbody>
      <% for( let i = 0; i < users.length; i++ ) { const user = users[i] %>
      <tr>
        <th scope="row"><%=i%></th>
        <td>
          <%- include('../includes/avatar', {user}) %>
        </td>
        <td><a href="/auth/<%= user.username%>">@<%= user.username %></a></td>
        <td><a href="mailto:<%= user.email %>"><%= user.email %></a></td>
        <td>
          <%- include('../includes/delete-modal', {pre:
          'auth',path:`${user.username}`}) %>
        </td>
      </tr>
      <% } %>
    </tbody>
  </table>
</div>
