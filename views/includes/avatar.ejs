<% if (user.avatar != null) { %>
<img
  src="/files/<%= user.avatar.filename %>"
  class="img-fluid rounded"
  alt="<%= user.avatar.key %>"
/>
<% } else {%>
<img
  src="/images/profile-image.jpg"
  class="img-fluid rounded"
  alt="profile-image"
/>
<% }%>
