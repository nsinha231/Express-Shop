<hr />
<div class="row justify-content-center bg-light pt-2">
  <div class="m-0 col-sm-5 col-md-4 col-lg-3 text-center">
    <img
      src="/files/<%= product.thumbnail.filename %>"
      style="width: 20rem;"
      class="img-fluid"
      alt="<%= product.thumbnail.filename %>"
    />
  </div>
  <div class="m-0 col-sm-10 col-md-8 col-lg-8">
    <a href="/<%= product.slug%>"> <h3><%= product.title %></h3></a>
    <p><%= product.meta_description %></p>
    <h4><%= product.price %> ₹</h4>
    <h4>
      <%= typeof product.quantity != 'undefined' ? product.quantity : '' %>
    </h4>
    <a href="/auth/<%= product.seller.username%>" class="w-100">
      <p class="mb-0 d-inline-block">By <%= product.seller.username %></p>
    </a>
  </div>
</div>
